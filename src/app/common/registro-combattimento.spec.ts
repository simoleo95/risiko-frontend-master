import { RegistroCombattimento } from './registro-combattimento';

describe('RegistroCombattimento', () => {
  it('should create an instance', () => {
    expect(new RegistroCombattimento()).toBeTruthy();
  });
});
